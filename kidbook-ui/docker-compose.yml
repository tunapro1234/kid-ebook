version: '3.8'

services:
  frontend:
    build: .
    container_name: kidbook-ui
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: always 